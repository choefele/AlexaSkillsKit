import Foundation
import AlexaSkillsKit

public class AlexaSkillHandler : RequestHandler {
    public init() {
        
    }
    
    public func handleLaunch(request: LaunchRequest, session: Session, next: @escaping (StandardResponse) -> ()) {
    }
    
    public func handleIntent(request: IntentRequest, session: Session, next: @escaping (StandardResponse) -> ()) {
        
    }
    
    public func handleSessionEnded(request: SessionEndedRequest, session: Session, next: @escaping () -> ()) {
        
    }
} 
